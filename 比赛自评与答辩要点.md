# KuiklyStock · 比赛自评与答辩要点

> 参赛：2026 腾讯犀牛鸟开源人才计划 · KuiklyUI 实战
> 项目：KuiklyStock，AI 股票行情 + AI 股票问答
> 用途：对着四大评分维度自证完成度，另有预设答辩 Q&A。给评委和答辩用，语气尽量务实。

---

## 一、交付三件套

| 交付项 | 状态 | 位置 |
| --- | --- | --- |
| ① 可编译运行代码 | ✅ 真机跑通 | 仓库根，入口 `MainTab`（底部四 Tab）。clone 下来零配置就能跑出真 AI，仓库自带共享的免费池 Key |
| ② 说明文档 | ✅ | `README.md`（总览 + 怎么跑）、`架构说明.md`、`亮点与创新.md`、`比赛自评与答辩要点.md`、`演示口播脚本.md`、`演示镜头时间轴录制卡.md` |
| ③ 演示视频 | ✅ 已录制 | 成片随交付附件提交（含一张补充截图，展示视频里没来得及演示的功能） |

---

## 二、四大维度自评（权重：功能 40 / 代码 25 / AI 25 / 加分 10）

### 功能 40，两大任务都闭环，覆盖面超出 Demo 的最低要求

**Task 01 · AI 行情原型（全链路可演示）**

- 行情：大盘（三大指数 / 热度 / 领涨领跌）+ 板块（行业 + 概念，200 多个，可搜索 / 关注）+ 个股榜单（涨幅 / 跌幅 / 换手 / 振幅 / 全部）
- 个股详情：真实 K 线（日 / 周 / 月 / 年可切）+ AI 分析卡 + 量化结论徽章 + F10 基本面 + 选点问 K
- 自选与多股对比：自选分组管理；对比页上区股票轮播（每页真迷你走势，可切周期）+ 下区对比 AI 聊天 + 独立选股页
- **数据是真的**：腾讯（报价 / K 线 / 分时）+ 新浪（榜单 / 板块 / 成分）+ 东方财富（F10），mock 只在离线兜底且明确标注

**Task 02 · AI 股票问答（核心闭环）**

- 真流式 SSE 逐字输出 + 候选模型降级 + 多轮上下文
- Markdown 富文本（标题 / 列表 / 引用 / 代码）+ 提及股 → 末尾可点迷你走势卡，文字里的股票名也能点进详情
- 详情页和聊天双端的量化徽章（操作建议 买入 / 持有 / 卖出 + 风险 低 / 中 / 高）
- 图表「看哪根 K 线就问哪根」：点图选点位 → 自动跳聊天、带着上下文追问
- 一键「开始选股」（强势题材 / 低估值 / 趋势突破）+「让 AI 解读基本面」

### 代码 25，自研组件解耦是亮点，规模和结构是短板

**亮点（自研可复用，不是 Demo 壳）**

- 49 个 .kt / shared 约 1.38 万行 + 宿主桥层 13 个 .kt
- 能脱离业务复用的组件：`KRStockList`（行内展开轮播）、`KRKLineChart` / `KRTrendChart`（图表）、`KRMarkdown`（富文本渲染器）、`KRChatMiniChart`（迷你图）
- 数据门面 `StockData` 统一封装腾讯 / 新浪 / 东财，桥层隔离，mock 兜底

**短板（自评诚实，答辩时可提改进方向）**

- `MainTabPager` 单文件偏大（主框架 + 行情 / 自选 / 我的 + 交互状态耦合在一起），可以再拆
- 缺一套全局 design token，部分颜色和间距散在各处（有 `UserSettings` 管主题色，但中性灰没收敛）
- 组件文档偏薄（`亮点与创新.md` 有一些说明，但没有逐组件的 API 文档）

### AI 25，真实大模型 + 独特的「AI 操作 App」Agent

- **真接 GLM 免费池，不是套壳**：SSE 流式、候选模型降级（限流自动切）、整体超时兜底
- **Prompt 有设计**：角色设定 + 输出格式约束 + 注入实时行情上下文 + 多轮历史
- **AI Agent：听懂人话就直接执行 App 操作**（本项目的差异点）
  - 说「把主题色改成蓝色 / 给茅台设跌破 1300 的预警 / 宁德时代加自选」，模型自己决策、真改 App 状态并持久化
  - 协议层全兜底：模型输出的 JSON 再怎么不规范（驼峰别名 / 内嵌 / 占位）都能解析，另有别名规范化映射和代码侧的语义校准（无单位金额按现价算绝对阈值）
  - 主聊天和股票对比页都能用同一套 Agent
- 如实说明：免费池高峰可能慢或降级到 mock，这不是模型能力问题

### 加分 10，真实数据源 + 新玩法，多端不硬凑

- 腾讯 / 新浪 / 东财多个真实数据源，避免单点依赖
- 自研图表与富文本、AI Agent 操作 App 的玩法
- 合规：致谢页列明 WorkBuddy、混元、GLM、各数据源、KuiklyUI、犀牛鸟
- **多端（鸿蒙 / H5 / 小程序）探明后如实放弃**：Kuikly 2.7.0 的 web 与鸿蒙内核在 Maven 上没有对应版本，鸿蒙还要 DevEco + 重写整套桥，Windows 单机跑不通。与其硬凑一个假多端，不如把 Android 端做到能演示，答辩可以主动说明这是「做过技术验证并给出结论」，而不是假装支持

---

## 三、自评得分参考

| 维度 | 权重 | 自评 | 理由 |
| --- | --- | --- | --- |
| 功能 | 40 | 35 | 两大任务全链路真实可演示，覆盖面超 Demo |
| 代码 | 25 | 19 | 组件解耦好，但有大文件、缺 token、文档薄 |
| AI | 25 | 21 | 真模型 + AI Agent 差异点 |
| 加分 | 10 | 8 | 真实数据源、新玩法，多端如实说明 |
| **合计** | 100 | **83** | 良好偏优，具备决赛竞争力 |

---

## 四、Q&A

**Q：AI 是接的真模型，还是写死的？**
A：真接智谱 GLM 免费池，SSE 真流式逐字输出，多轮上下文。没 Key 或限流时自动降级候选模型，或者落本地兜底。现场可以演示「字逐个蹦出来」和「回答跟着行情变」。

**Q：行情数据哪来的，是不是假的？**
A：腾讯（报价 / K 线 / 分时）+ 新浪（榜单 / 板块 / 成分）+ 东方财富（F10）三个真实源。App 里有数据来源标注，只有离线才落 mock 并且明示。北交所这类源里没有的数据会如实占位，不拿假波浪冒充。

**Q：你最有特色的点是什么？**
A：两个。一是「图表上选一点 → AI 解读这一点」的交互闭环；二是 AI Agent，它不只回话，你说「设个跌破 1300 的预警」，它真去改 App、改主题色、加自选、设预警并持久化。难的地方是 glm-4-flash 输出不稳定，我们做了协议层全兜底。

**Q：为什么没做鸿蒙 / 多端？**
A：做过技术验证。Kuikly 2.7.0 的 web 渲染内核在 Maven 上最高只到 2.4.0，没有 2.7.0 对应版本；鸿蒙要 DevEco 加重写整套 ArkTS 桥。与其硬凑一个跑不通的多端，不如把 Android 端打磨到可演示的完整度。这是有意的取舍。

**Q：代码规模多大，下一步想改什么？**
A：49 个文件、约 1.38 万行。自研组件已经解耦可复用。要继续的话会拆主框架大文件、补统一 design token、给组件写 API 文档、列表做窗口化渲染。

---

## 五、交付状态

- [x] 代码已提交并 push 到 GitHub（`zeriehan/KuiklyStock`，main）
- [x] README 补了「克隆下来怎么跑」，含 `local.properties` 与可选 GLM Key 说明
- [x] 演示视频已录制，另有截图补充视频未演示的功能
- [x] 真机回归：Agent 各类操作（加自选 / 对比 / 预警 / 主题色 / 字号 / 迷你卡 / 涨跌配色 / 隐藏恢复 / 恢复隐藏）+ 四 Tab 主流程
