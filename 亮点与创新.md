# 亮点与创新（Highlights & Innovation）

> 交付展示用。对照评分维度（功能/代码质量/AI 场景/加分），提炼本项目值得讲、评委能"眼前一亮"的点，供写说明与录演示时取舍。

## 一、真实数据 + 真实大模型（不是演示壳）

- **行情真实**：自研 `StockData` 门面，mock 种子保底 + 经宿主桥动态并入**东方财富/腾讯真实行情**（榜/板块/成分/K线/分时），断网或接口失败才回落 mock——既有真实观感又不至于空屏。
- **AI 真实**：聊天/详情页的 AI 全部走**智谱 GLM Flash 免费池真实链路**（GLM_API_KEY 走 local.properties 注入，源码零密钥）；未配 Key/限流才回退本地 Mock。
- **红涨绿跌**：股票涨跌配色严格遵循中国股市惯例（涨红 `0xFFE54D42`、跌绿 `0xFF1ABE5B`），且可在设置里切换红跌绿涨。

## 二、AI 回答"逐字蹦出"（真流式）

- 接入 GLM **SSE 流式**，AI 回答在聊天页像 ChatGPT 一样边生成边出现，不再是"干等整段"。
- 关键工程：发现 Kuikly 桥单次回调不透传多次后，改用**「宿主写缓存 + shared 轮询拉增量」**这条可靠通道，让流式在单回调 RPC 架构下成立；含模型候选降级（4.7→4.5→4-flash）与 15s 整体超时兜底（超时回 Mock，不无限等）。

## 三、"AI 说的 → 能点"的发散性承接（Task02 核心场景）

- AI 回复经 `StockMention` 扫描，提及的真实行情股 → **回答末尾自动生成迷你走势卡片 + Markdown 富文本内的股票名可点** → 点击跳转该股详情承接页。
- 自研 `KRMarkdown`：解析器(core)与渲染器(components)解耦，用 Kuikly 原生 RichText+Span 实现标题/列表/引用/代码/行内加粗与可点股票。同一份 Markdown 解析器可复用。

## 四、⭐ "图表上选一点 → AI 解读这一点"（交互闭环，本项目最有新意的一点）

- **把"看盘"和"问 AI"揉进同一个手势**：分时图或 K 线上**点一下/长按拖动** → 吸附出十字光标（定位到那一根 K 线/那一个时间点）→ 图上即浮现该点的 时间/价/涨跌 → 一个「就这点问」按钮 → 点它就把**带着这个具体点位上下文的追问**交给 AI（跳该股聊天页自动发出）。
- **追问不是空泛的**：模型收到的是真实点位数据，例如「贵州茅台 03-12 日K 收 1728.50、涨跌 +2.1%，这一根线为什么这么走？」——AI 能对着这根线回答，实现"看哪问哪"的图表级对话。
- **两处同源可用**：
  1. **AI 回复内嵌的迷你走势图**（`KRChatMiniChart`）：模型主动按 `[KCHART:代码:周期]` 指令在回复中插图表；图顶可切 分时/日/周/月/年K。
  2. **个股详情的专业 K 线主图**（`KRKLineChart`）：含 MA/指标副窗/缩放，选点后同样"就这点问"承接。
- **工程价值**：一套"吸附索引 → 取该点 OHLC/价 → 拼追问 → 复用聊天 prompt 自动发送"的通用回调，组件不绑股票上下文、只透传点位，可复用到任何图上。
- **为什么新**：绝大多数行情/AI 产品是"图表看图、聊天打字"割裂的两步；这里用户盯着某根 K 线就能让 AI 就这根线展开解读，交互直觉且自然，是最容易让评委"哦"一声的 demo 点。

## 五、AI 结论量化：一句"风险 + 买卖"直达金融产品感

- **不是空谈看多/看空，AI 分析给可执行结论标签**：个股详情 AI 分析卡顶部，用两个醒目的"徽章"直给 **风险档（低/中/高）+ 操作建议（买入/持有/卖出）**——绿=低风险/卖出、橙=中风险、红=高风险/买入，一眼读懂"该不该买、风险多大"，观感像同花顺/雪球个股页的评级。
- **怎么让模型"量化"又不加一次数据请求**：模型做判断用的量价数据（现价/涨跌/高低/成交量/日K）本就已喂进 prompt；只要求它在分析最开头**独占一行输出严格格式** `【AI观点】风险：中风险｜操作建议：持有`，shared 用正则解析 → 渲染成徽章，正文里剥掉这行不重复。
- **双兜底保证"永远有结论可展示"**：真 GLM 不按格式 → 解析空、仅显示正文不崩；GLM 限流/离线落到本地 Mock 时，Mock 也按同一格式补一行结论——演示时不会出现"没徽章"的尴尬。

## 六、稳定性设计：离线永不卡死

- **LLM 双实现兜底**：真模型失败/超时/未配 Key → 自动切本地 `MockLLMClient`，AI 卡片与聊天都有内容，不出现"分析中…"永远转圈。
- **数据兜底**：真实榜单/板块拉取失败保留 mock，四 Tab 常驻。
- **后台继续跑**：LLM 请求挂在常驻根页桥（`AIJobCenter`），退出聊天/详情页回复不丢；会话/结论落盘 SharedPreferences，冷启动可恢复。

## 七、代码质量（自评拿分点）

- **一套 KMP shared**：UI+业务全在 commonMain，Android 宿主只做桥，分层清晰（app/components/core/base）。
- **组件可复用**：KRTable/KRKLineChart/KRTrendChart/KRMarkdown 等自研组件数据驱动、脱离具体业务；KRMarkdown 解析与渲染解耦。
- **严格约定沉淀**：Kuikly 高频坑（响应式 vif/attr、padding 4 边对称、桥单回调、页面定时器绑前台页等）在代码注释与工程记忆里均有总结，工程演进少走弯路。

## 八、可展示的交互亮点（录演示建议覆盖）

1. AI 聊天发问 → **文字逐字蹦出** → 末尾提及股卡片 → 点卡片进详情。
2. 行情/自选列表 → 行内展开（分时/AI 分析/简况三页）→ 进个股详情看真实 K 线与 AI 分析。
3. 大盘/板块/个股真实榜单与涨跌红绿配色。
4. 我的：外观个性化（主题色/字号/涨跌配色）、迷你卡片开关、自选/隐藏管理。
5. ⭐ **图表上选一点 → AI 解读那一点**：迷你图/详情 K 线点出十字光标 →「就这点问」→ AI 针对该根 K 线/该时刻解读（见第四节，强烈建议单拍一段）。
6. 行情·个股页 **「开始选股」一键直发**：点按钮 AI 自动做一次选股（题材/低估值/趋势多风格各荐几只）。
7. 输入框**草稿**：打一半退出再进不丢；输入框带键盘顶起优化。
8. **AI 分析卡的风险/买卖徽章**：详情页 AI 分析标题下两个醒目徽章（如「高风险·卖出」）直给可执行结论（见第五节，单拍特写最出效果）。
9. **⭐ 股票对比页（Task01 延伸）**：AI Tab 顶「股票对比」→ 上区横滑对比股（每页真迷你走势可切分时/日K）+ 下区对比 AI 聊天 → 问「谁更值得持有」得结构化对比解读（见第九节，强烈建议录一段）。

## 九、⭐ 多股对比：上股票轮播 + 下对比 AI 聊天

> Task01 的延伸创新：把"对比"做成真正的产品形态，而不是"打开两只详情页自己看"。

**产品形态（AI Tab 顶「股票对比」入口进入）**：
- **上区 ~2/5**：横滑对比，一只股票占一页，每页紧凑卡（**股票名为红下划线可点击跳个股详情**，模仿聊天的提及股卡片样式 + 实时价 + 涨跌幅 + **真迷你走势图**，可切 **分时 / 日K / 周K / 月K / 年K**，每只股票的周期独立）。最右一页为「+」追加新股票。
- **下区 ~3/5**：针对当前对比股列表的**对比 AI 聊天**——发问后实时价 + 近 20 日收盘会被喂进定制 prompt，模型给出结构化对比解读（KRMarkdown 富文本渲染，输入框颜色已对齐避免 Kuikly 隐性坑）。
- **顶部「设置」**：跳独立选股页（**复刻行情页**：搜索框 + 榜单 Tab「涨幅/跌幅/换手/振幅/全部」+ 每行 market 风格行尾「＋加入/✓移除」点行即加 + 顶部已加 chips 可×删）。选股页「完成」落盘对比股 → 对比页 `pageDidAppear` 自动重读并应用，**无需手动刷新**。

**技术亮点**：
- 迷你走势区直接**复用自选展开同款 KRMiniTimeSharing**（分时，122 高，与自选/行情列表里看到的迷你图一致），日/周/月/年K 用 KRTrendChart 收盘价趋势线——**完全复用现有图表组件，无新绘制代码**。
- 上区每只股紧凑卡（170 高），走势区对标自选展开迷你图尺寸（122 高），不挤占下方聊天空间。
- 对比 AI prompt 不复用 LLM.client.chat 的"个股自由问答"流（那流硬绑 Stock 上下文），而是**直接用 bridgeModule.llmAnalyze 自定义 prompt**：列出对比股名称/代码/实时价/涨跌/近 20 日收盘 + 用户问题，模型据此回答；不限流/无 Key 时本地兜底提示。
- 跨页（对比页 ↔ 选股页）回传：选股页「完成」把最终 codes 写 **SharedPreferences**（持久化）并 `closePage`；对比页 **override `pageDidAppear()`**（Kuikly Pager 的"页面重新出现"回调）自动重读持久化并应用——**实现"完成即自动生效"**，规避了此前"无可靠子页 close→父页 resume、需手动应用选股"的笨拙方案。`initialCodes` 走 `openPage` 的 `pageData` 直接传（Kuikly 官方跨页传参方式）。

**复用 + 解耦**：所有图表组件（KRMiniTimeSharing / KRTrendChart / KRMarkdown）零修改复用；新增 StockComparePage / StockPickerPage / ComparePicker 三个文件，按"页面级 ViewBuilder"组织，未污染已有模块。

**对比页内的"股票名→详情"（红下划线）**：迷你走势卡片股票名用 `RichText+Span`（主题色+`textDecorationUnderLine`+click→openPage StockDetail），与聊天的提及股卡片视觉一致——"看到一只股 → 一键去看它"，形成"列表 → 对比 → 详情" 的贯通闭环。

## 十、⭐ AI Agent：听懂人话，直接执行 App 操作（9/7 新增，最强实操亮点）

> 把"AI 会说话"再往前推一步——用户用**自然语言**（"把主题色改成蓝色"、"给茅台设个跌破 1300 的预警"、"宁德时代加进自选"）就能让 AI **真改这个 App**，而不只是回一段文字。

**形态**：AI 聊天里发一句操作指令 → 模型自己**判断这是不是要执行操作** → 是则输出一行 `⟦TOOL⟧{json}`（含工具名+参数）→ `executeTool` 在 shared 侧真实改 `UserSettings` / 自选 / 预警 / 对比并落盘 → 文案告知"已执行"。

**为什么难、做了什么（协议层全兜底）**：真实调 GLM 时，小模型输出的 JSON **极不稳定**，本项目做了逐层兜底，任何形态都能正确识别执行：
1. **JSON 解析容错**（`ToolArgs`）：支持标准 `{name,args}`、`{tool/function/operation/action+args}` 驼峰字段、模型把 name 当外层 key（`{"addAlert":{...}}`）、递归拍平内嵌 args、字面量 `{json}`/空 `{}` 占位过滤——不再对"格式不标准"报错。
2. **别名规范化 canonicalizeToolName**：`change_theme_color`/`set_color`/`tool` 等任意含 theme/color/色/font/字体/迷你/card/配色/watch/compare/alert 的别名都映射回内部精确工具名；未命中返回空（不误当工具名）。
3. **语义兜底在 executeTool 代码侧**（不只靠 prompt，glm-4-flash 照抄少样本倾向强、样本必须自洽）：
   - 无单位金额（"加跌100的预警"）→ 注入实时价表，模型按现价算绝对阈值 `threshold=现价∓金额`；再用用户原话 query 二次校准，杜绝把"跌100元"误解成"当日跌幅 100%"这种不可能条件。
   - 失败短路：执行结果含"未/失败/无法"直接回准确错误，不让模型复述包装成模糊话术。
4. **少样本 = 规则示范必须自洽**：prompt 里每个操作给一句真实用户话 → 一行 `⟦TOOL⟧{json}` 的示范，与文字规则一致，避免模型照抄错误示范。

**可演示的 Agent 指令（真执行、落盘可查）**：改主题色（说"改红/蓝"）、改字号（"字体调大"）、换涨跌配色（"换成欧美配色"）、开关迷你卡组件（"把展开卡里的分时走势关掉"）、把某股加自选/加入对比、设价格预警（跌破/涨破/当日涨幅≥/当日跌幅≥，可一次说清金额或百分比）、设「不感兴趣」自动恢复天数、恢复被隐藏的股票。改完"返回主界面/对应设置页"即可看到变化——**不是"假装执行"，是真改了状态并持久化**。

**一处诚实说明**：预警类型切换（4 chip 单选配一种）中途发现用户更期望"4 行并排一次配多种"，已改版成 4 行独立输入 + 一次保存；回显曾因依赖 Input ref 时序不稳定，最终按 Kuikly 正确姿势用声明式 `attr{ text(...) }` 注入草稿解决——这两点是踩坑后的产品化修正，也沉淀成了 Kuikly 的输入铁律。

## 诚实备注（避免演示被当 bug）

- ~~聊天键盘弹出不自动顶起内容~~（已优化：输入栏通过占位 Spacer 随键盘顶起；沉浸式 adjustNothing 下顶起是主动实现而非 bug）。
- 北交所/新股历史日K：当前免费行情源不提供，详情页对该类股票显示诚实占位（分时与实时价真实），不拿假波浪冒充。
- AI 延迟取决于 GLM 免费池，高峰可能慢/限流(自动降级/兜底)；**演示建议在 GLM 通畅时录**，避免频繁落 Mock 造成结论与实时行情脱节。
