# 亮点与创新（Highlights & Innovation）

> 交付展示用。对着评分维度（功能 / 代码质量 / AI 场景 / 加分）挑出真正值得讲、评委会有反应的点，写说明和录演示时按需取舍。

## 一、真实数据 + 真实大模型

- **行情是真的**：自研 `StockData` 门面，mock 种子保底，经宿主桥动态并入腾讯 / 新浪 / 东财的真实行情（榜单、板块、成分、K 线、分时）。断网或接口挂了才回落 mock，不至于空屏。
- **AI 也是真的**：聊天和详情页的 AI 全部走智谱 GLM Flash 免费池的真实链路（Key 由宿主侧注入，仓库里带了一个共享的免费池 Key，clone 下来零配置就能用；shared 代码里没有任何密钥字符串）。没 Key 或限流才回退本地 Mock。
- **涨红跌绿**：涨 `0xFFE54D42`、跌 `0xFF1ABE5B`，按中国股市惯例；设置里还能切成红跌绿涨。

## 二、AI 回答「逐字蹦出」

- 走 GLM 的 **SSE 流式**，回答像 ChatGPT 一样边生成边出现，不用干等整段。
- 工程上的关键一步：Kuikly 的桥单次回调传不了多次，所以改成**「宿主写缓存 + shared 定时轮询拉增量」**，让流式在单回调 RPC 架构下也能成立。候选模型按 glm-4-flash → 4.5-flash → 4.7-flash 依次降级，整体超时兜底回 Mock，不无限等。

## 三、「AI 说的 → 能点」（Task02 核心场景）

- AI 回复经 `StockMention` 扫一遍，提到的真实行情股会在**回答末尾生成迷你走势卡片**，同时 Markdown 富文本里的**股票名可以点** → 跳该股详情页承接。
- 自研 `KRMarkdown`：解析器（core）和渲染器（components）解耦，用 Kuikly 原生 RichText + Span 实现标题 / 列表 / 引用 / 代码 / 行内加粗和可点股票。解析器可以单独复用。

## 四、⭐ 「图表上选一点 → AI 解读这一点」（交互闭环，本项目最有新意的一处）

- **把「看盘」和「问 AI」压进同一个手势**：分时图或 K 线上点一下、或长按拖动 → 吸附出十字光标（定位到那一根 K 线 / 那个时间点）→ 图上浮出该点的时间 / 价 / 涨跌 → 一个「就这点问」按钮 → 点它，就带着**这个具体点位**的上下文跳到该股聊天页自动发问。
- **追问带着真实数据**：模型收到的是真实点位，比如「贵州茅台 03-12 日 K 收 1728.50、涨跌 +2.1%，这一根为什么这么走？」，它能对着这根线回答，做到看哪问哪。
- **两个地方都能用**：
  1. AI 回复内嵌的迷你走势图（`KRChatMiniChart`），模型按 `[KCHART:代码:周期]` 指令主动插图，图顶可切分时 / 日 / 周 / 月 / 年 K。
  2. 个股详情的专业 K 线主图（`KRKLineChart`），带 MA / 指标副窗 / 缩放，选点后同样「就这点问」。
- **可复用**：一套「吸附索引 → 取该点 OHLC / 价 → 拼追问 → 复用聊天 prompt 自动发送」的回调，组件不绑股票上下文，只透传点位，换个图也能用。
- 多数行情产品是「图表看图、聊天打字」两步走；这里用户盯着某根 K 线就能让 AI 就这根展开，是演示时最容易让人「哦」一声的点。

## 五、AI 结论量化：风险 + 买卖两个徽章

- **给可执行结论，不只给一段文字**：个股详情 AI 分析卡顶部放两个徽章，直给**风险档（低 / 中 / 高）+ 操作建议（买入 / 持有 / 卖出）**，一眼看懂「该不该买、风险多大」，观感接近同花顺 / 雪球个股页的评级。
- **不加额外请求**：判断用的量价数据（现价、涨跌、高低、成交量、日 K）本来就已经喂进 prompt，只要求模型在分析开头**独占一行**输出 `【AI观点】风险：中风险｜操作建议：持有`，shared 用正则解析成徽章，正文里把这行剥掉不重复。
- **双兜底**：真 GLM 不按格式走，就只显示正文，不崩；限流或离线落到 Mock 时，Mock 也按同一格式补一行结论，演示不会出现「没徽章」的尴尬。

## 六、稳定性：离线也不卡死

- **LLM 双实现兜底**：真模型失败 / 超时 / 没配 Key，自动切本地 `MockLLMClient`，分析卡和聊天都有内容，不会「分析中…」一直转圈。
- **数据兜底**：真实榜单 / 板块拉取失败就保留 mock，四个 Tab 照常。
- **后台继续跑**：LLM 请求挂在常驻根页桥（`AIJobCenter`），退出聊天或详情页回复也不丢；会话和结论落盘 SharedPreferences，冷启动能恢复。

## 七、代码质量（自评拿分点）

- **一套 KMP shared**：UI 和业务全在 commonMain，Android 宿主只做桥，分层清楚（app / components / core / base）。
- **组件可复用**：KRTable、KRKLineChart、KRTrendChart、KRMarkdown 等自研组件数据驱动，不绑具体业务；KRMarkdown 解析与渲染解耦。
- **约定有沉淀**：Kuikly 那些高频坑（响应式 vif / attr、padding 四边对称、桥单次回调、定时器绑前台页等）在代码注释和工程记忆里都记着，后面少走弯路。

## 八、录演示建议覆盖的交互

1. AI 聊天发问 → 文字逐字蹦 → 末尾提及股卡片 → 点卡片进详情。
2. 行情 / 自选列表行内展开（分时 / AI 分析 / 简况）→ 进个股详情看真实 K 线与 AI 分析。
3. 大盘 / 板块 / 个股的真实榜单与涨跌红绿。
4. 「我的」里的外观个性化（主题色 / 字号 / 涨跌配色）、迷你卡开关、自选与隐藏管理。
5. ⭐ 图表选一点 → AI 解读那一点（见第四节，建议单独拍一段）。
6. 行情 · 个股页「开始选股」一键直发，AI 按题材 / 低估值 / 趋势各荐几只。
7. 输入框草稿：打一半退出再进不丢；输入框随键盘顶起。
8. ⭐ AI 分析卡的「风险 / 买卖」徽章（见第五节，单拍特写最出效果）。
9. ⭐ 股票对比页（见第九节）。

## 九、⭐ 多股对比：上股票轮播 + 下对比 AI 聊天

> Task01 的延伸。把「对比」做成一个产品形态，而不是让人开两个详情页自己看。

**形态（AI Tab 顶部「股票对比」进入）**

- **上区（约 2/5）**：横滑翻页，一只股票一页，紧凑卡上是**红下划线的股票名（可点进详情）**、实时价、涨跌幅，以及**真迷你走势图**，周期可切 分时 / 日 K / 周 K / 月 K / 年 K，每只股独立。最后一页是「+」添加新股票。
- **下区（约 3/5）**：针对当前对比股列表的**对比 AI 聊天**，答完可以逐字看它生成，也能在这里直接让 AI 干活（加自选 / 加对比 / 设预警 / 改设置，和主聊天同一套 Agent）。
- **顶部「设置」**：跳独立选股页（搜索、涨幅 / 跌幅 / 换手 / 振幅 / 全部五个榜单、行尾「＋加入 / ✓移除」、顶部已加 chips 可删）。点「完成」落盘，对比页重新出现时自动重读并应用，不用手动刷新。

**做法上的几个取舍**

- 迷你走势区直接复用自选展开同款的 `KRMiniTimeSharing`（分时，122 高），日 / 周 / 月 / 年 K 用 `KRTrendChart` 画收盘趋势线，没写新的绘制代码。
- 对比 prompt 不复用 `LLM.client.chat` 的个股问答流（那条硬绑单个 Stock 上下文），而是自己拼多股上下文：名称、代码、实时价、涨跌、区间高低 + 期间涨跌幅。早先版本把每只股近 20 天收盘价逐个列出来，prompt 太大，免费池高峰容易限流，已经瘦身成 10 日窗口 + 区间，不再列逐日价格。
- 对比页走 `AIJobCenter` 的流式通道（和主聊天一样），而不是非流式，非流式那条有 15s 整体硬超时，会把「慢但能用」的 glm-4-flash 误判成失败。
- 跨页回传（对比页 ↔ 选股页）：选股页「完成」把最终 codes 写进 SharedPreferences 再 `closePage`，对比页 override `pageDidAppear()` 自动重读并应用，实现「完成即生效」。`initialCodes` 走 `openPage` 的 `pageData` 直接传。

**复用情况**：图表组件（KRMiniTimeSharing / KRTrendChart / KRMarkdown）零修改复用；新增 StockComparePage、StockPickerPage、ComparePicker 三个文件，按页面级 ViewBuilder 组织，没污染已有模块。

**对比页里「股票名 → 详情」**：迷你卡上的股票名用 `RichText + Span`（主题色 + 下划线 + 点击跳详情），和聊天的提及股卡片视觉一致，形成「列表 → 对比 → 详情」的贯通。

## 十、⭐ AI Agent：听懂人话，直接执行 App 操作（最强实操亮点）

> 把「AI 会说话」向前推一步，用自然语言（「把主题色改成蓝色」「给茅台设个跌破 1300 的预警」「宁德时代加进自选」）就能让 AI 真改这个 App。主聊天和股票对比页都能用。

**流程**：AI 聊天里发一句操作指令 → 模型自己判断是不是要执行操作 → 是就输出一行 `⟦TOOL⟧{json}`（工具名 + 参数）→ `executeTool` 在 shared 侧真实修改 `UserSettings` / 自选 / 预警 / 对比并落盘 → 回一句「已执行」。

**难在哪、做了什么**：真调 GLM 时小模型输出的 JSON 极不稳定，所以逐层做了兜底，什么形态都能认出来：

1. **JSON 解析容错**（`ToolArgs`）：标准 `{name,args}`、`{tool/function/operation/action + args}` 驼峰字段、模型把工具名当外层 key（`{"addAlert":{...}}`）、内嵌 args 递归拍平、字面量 `{json}` 和空 `{}` 占位过滤。
2. **别名规范化**：`change_theme_color`、`set_color`、`tool` 这类别名，只要含 theme / color / 色 / font / 字体 / 迷你 / card / 配色 / watch / compare / alert 等线索，都映射回内部精确工具名；没命中就返回空，不误当工具名。
3. **语义兜底放在代码侧**（不只靠 prompt，glm-4-flash 照抄少样本的倾向很强）：
   - 无单位金额（「加跌 100 的预警」）注入实时价表，模型按现价算绝对阈值；再用用户原话二次校准，避免把「跌 100 元」理解成「当日跌幅 100%」这种不可能的条件。
   - 执行结果里含「未 / 失败 / 无法」就直接把准确错误返回，不让模型复述成模糊话术。
4. **少样本必须自洽**：prompt 里每个操作给一句真实用户话 + 一行正确示范，和文字规则保持一致，避免模型照抄错误示范。

**可以演示的指令**（真执行、落盘可查）：改主题色、改字号、换涨跌配色、开关迷你卡组件、加自选 / 加对比、设价格预警（跌破 / 涨破 / 当日涨幅≥ / 当日跌幅≥，可以一次说清金额或百分比）、设「不感兴趣」的自动恢复天数、恢复被隐藏的股票。改完回主界面或对应设置页就能看到变化，不是假装执行。

**一处如实说明**：预警类型切换原本是「4 个 chip 单选配一种」，后来发现用户更想要「4 行并排一次配多种」，就改成了 4 行独立输入 + 一次保存；回显一度依赖 Input ref 的时序，不稳定，最后按 Kuikly 的正确姿势用声明式 `attr { text(...) }` 注入草稿解决。这两处都是踩坑后的产品化修正，也沉淀成了输入相关的铁律。

## 如实备注（免得演示时被当成 bug）

- 键盘顶起：输入栏靠后置占位 Spacer 随键盘上移；沉浸式 `adjustNothing` 下这是主动实现的，不是没做。
- 北交所 / 新股的历史日 K 免费源没有，详情页如实占位（分时和实时价是真的），不拿假波浪冒充。
- AI 延迟看 GLM 免费池的脸色，高峰可能慢或落 Mock；**演示建议在 GLM 通畅时录**，免得结论和实时行情对不上。
