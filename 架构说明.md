# 架构说明（Architecture）

> 配套交付文档之一。项目主 README（`README.md`）承载产品/进度/协作说明，本文聚焦技术架构。
> 代码基线：`shared/src/commonMain/kotlin/com/zeriehan/kuiklystock/`（KMP shared 业务代码）+ `androidApp/`（Android 宿主桥）。

## 1. 总体分层

```
┌─────────────────────────────────────────────────────────────┐
│ UI 层 app/        四 Tab 主框架 + 9 个 @Page 子页面           │
│   chat / quotes / detail / mine                              │
├─────────────────────────────────────────────────────────────┤
│ 组件层 components/   自研可复用组件(表格/卡片/K线/Markdown…)   │
├─────────────────────────────────────────────────────────────┤
│ 业务核心 core/       Stock(模型)/StockData(门面)/行情/偏好存储 │
│   └ core/llm          LLM 客户端 + AI 会话/卡片/任务中心       │
├─────────────────────────────────────────────────────────────┤
│ 基础桥 base/         BasePager/BridgeModule(shared→宿主 RPC)  │
├─────────────────────────────────────────────────────────────┤
│ 宿主 androidApp/     KuiklyRenderActivity + KRBridgeModule    │
│   (网络/GLM/内存/日志/路由 adapter)                            │
└─────────────────────────────────────────────────────────────┘
```

- **shared（KMP commonMain）**：全部业务逻辑与 UI，用 Kuikly DSL 写页面，一套代码可跑多端（工程默认 Android）。
- **androidApp（宿主）**：Kuikly 渲染引擎 + JVM 宿主桥。凡是 shared 无法直接做的（真实 HTTP、GLM 大模型、系统能力）由宿主桥承接，**shared 零密钥、零网络直连**。

## 2. 跨层通信：桥（BridgeModule）与 Adapter

shared 不直接访问 Android API，统一经 `base/BridgeModule`（`callNativeMethod` RPC）下发命令，宿主 `KRBridgeModule.call()` 分发执行并单次回调：

| shared 侧方法 | 宿主处理 | 用途 |
|---|---|---|
| `fetchQuotes(secids)` | 腾讯行情接口 | 真实行情并入 |
| `fetchClist/fetchSectors/fetchSectorStocks` | 东财榜/板块/成分 | 榜单、板块页 |
| `fetchKline/fetchTrends` | 腾讯K线/东财分时 | 图表数据 |
| `llmAnalyze(prompt,stream,sid)` | 智谱 GLM | AI 生成(可流式) |
| `showSelectableText/copyToPasteboard/toast` | 系统能力 | 复制/提示 |

其它基础能力（颜色/字体/图片/日志/线程）通过 `adapter/*`（`KRColorParserAdapter` 等）注册给渲染引擎。

## 3. 数据模型与数据源（core/）

- `Stock.kt`：`Stock`/`KLineBar`/`Sector` + 涨跌配色（`StockColor`：涨 `0xFFE54D42` 红、跌 `0xFF1ABE5B` 绿，符合中国股市习惯）+ 指标 MA/EMA/MACD/RSI/BOLL。
- `StockData.kt`（门面）：`baseQuotes`（mock 种子兜底）+ `realPool`（经桥拉真实行情动态并入）。`loadRank/loadSectors/loadSectorStocks` 失败时保留 mock，保证演示链路不空。`DataSync.bump()` 驱动重绘。
- `UserStockStore.kt`：自选(CSV)/隐藏，`UserSettings.kt`：主题色/字号/深色/涨跌配色偏好，持久化。
- `KRMarkdown.kt`：Markdown 解析器（块 + 行内 token）。

## 4. AI 与 LLM 子系统（core/llm/）— 重点

```
UI → LLM.client(LLM.kt)
      ├─ GLMFlashClient(真实智谱) —— 走 AIJobCenter(常驻根页桥)
      │     ├─ analyze(): 详情页个股 AI 分析(纯文本【】分段)
      │     └─ chat():     聊天页多轮问答(流式/非流式)
      └─ MockLLMClient(离线兜底, 零网络)
宿主 KRBridgeModule.llmAnalyze → glm-4.7→4.5→4-flash 候选链 + 15s 整体兜底
```

- **LLMClient 抽象 + 双实现**：真模型 `GLMFlashClient` 与离线 `MockLLMClient` 同接口，未配 Key/网络失败自动回退，演示永不卡死。
- **AIJobCenter 单例**：LLM 请求统一走「常驻根页面(MainTabPager)」的桥 → 子页面(聊天/详情)销毁后请求照常飞行、结果写单例 store 不丢（"后台继续跑"）。
- **真流式(SSE)**：宿主读流把累计文本写入按 `sid` 的缓存，shared 用绑前台页的定时器轮询取增量，聊天气泡"逐字蹦出"；桥单次回调不透传多次，故用"宿主缓存+轮询拉"这一可靠通道。
- **持久化**：`ChatStore`(会话按股票隔离)、`AIAnalysisStore`(详情页 AI 结论) 均落盘 SharedPreferences。
- **发散性渲染**：`StockMention.extract` 扫 AI 文本命中行情池股票 → 末尾渲染可点迷你走势卡片 + Markdown 富文本内股票名可点 → 承接跳个股详情（Task02 闭环）。
- **图表选点 → AI 解读（反方向承接）**：
  - AI 回复按 `[KCHART:代码:周期]` 指令内嵌迷你走势图（`KRChatMiniChart`，分时/日/周/月/年K 可切）；个股详情主图 `KRKLineChart` 亦同。
  - 两图点按吸附十字光标 → 组件把**被选点(时间/价/涨跌/是否分时)** 经 `onAsk` 回调透传给外层（组件不绑股票上下文，只传点位，保持可复用）→ 外层拼成完整追问 → `RouterModule.openPage("Chat", {stockCode, prompt})` → ChatPage 收到 `prompt` 自动 `ask()` 发出。
  - 结果：用户"看哪根 K 线，就点那根问 AI 解读那根"，实现图表级对话承接。

## 5. 页面与导航（app/）

- 四 Tab 主框架 `MainTabPager`（AI 聊天/行情/自选/我的），子 Tab 内容用 `flex+opacity` 常驻显隐。
- 业务 `@Page`：`QuotesPage`、`StockDetailPage`、`SectorDetailPage`、`ChatPage`、`HeatPoolPage`、`AppearancePage`、`ExpandSettingsPage`、`HiddenStocksPage`（新页由 KSP 自动注册）。
- 导航经 `RouterModule.openPage(name, userData)`；详情/聊天/承接均由行情或卡片点击进入，页面参数 `stockCode`/`prompt` 驱动。

## 6. 自研可复用组件（components/）

| 组件 | 职责 | 复用性 |
|---|---|---|
| KRTable(KRStockList) | 行情列表 + 行内展开 3 页轮播(分时/AI分析/简况) | 脱离业务数据可复用 |
| KRKLineChart / KRMiniTimeSharing / KRTrendChart | 专业K线蜡烛(+指标副窗/缩放) / 迷你分时 / 迷你走势 | 纯数据驱动 |
| KRChatMiniChart | AI 回复内嵌迷你走势图(分时/日/周/月/年K 可切) + 十字光标选点 | `onAsk` 透传点位，不绑股票上下文 |
| KRStockCard / KRStockBadge / KRRefreshButton | AI 提及股卡 / 涨跌徽标 / 下拉刷新 | 通用 |
| KRMarkdown(KRMarkdownView) | Markdown 富文本(标题/列表/引用/代码/可点股票) | 解析器(core)+渲染器解耦 |

## 7. 宿主 Android 侧关键约定

- `windowSoftInputMode=adjustNothing`(沉浸式 LAYOUT_FULLSCREEN, 内容顶到状态栏后)。
- GLM Key 由 `local.properties` 经 `BuildConfig.GLM_API_KEY` 注入，源码零密钥。
- 真实K线走腾讯、分时/榜单/板块走东方财富（跨域策略下选可用域）。
