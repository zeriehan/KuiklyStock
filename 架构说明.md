# 架构说明（Architecture）

> 交付文档之一。产品、进度、协作看 `README.md`，本文只讲技术架构。
> 代码基线：`shared/src/commonMain/kotlin/com/zeriehan/kuiklystock/`（KMP shared 业务代码）+ `androidApp/`（Android 宿主桥）。

## 1. 总体分层

```
┌─────────────────────────────────────────────────────────────┐
│ UI 层 app/        四 Tab 主框架 + 13 个 @Page 子页面          │
│   chat / quotes / detail / compare / mine                    │
├─────────────────────────────────────────────────────────────┤
│ 组件层 components/   自研可复用组件(表格/卡片/K线/Markdown…)   │
├─────────────────────────────────────────────────────────────┤
│ 业务核心 core/       Stock(模型)/StockData(门面)/行情/偏好存储 │
│   └ core/llm          LLM 客户端 + AI 会话/卡片/任务中心       │
├─────────────────────────────────────────────────────────────┤
│ 基础桥 base/         BasePager/BridgeModule(shared→宿主 RPC)  │
├─────────────────────────────────────────────────────────────┤
│ 宿主 androidApp/     KuiklyRenderActivity + KRBridgeModule    │
│   (网络/GLM/内存/日志/路由 adapter)                            │
└─────────────────────────────────────────────────────────────┘
```

- **shared（KMP commonMain）**：全部业务逻辑和 UI 都用 Kuikly DSL 写，一套代码可跑多端（当前工程只配 Android）。
- **androidApp（宿主）**：Kuikly 渲染引擎 + JVM 宿主桥。shared 做不了的事（真实 HTTP、GLM、系统能力）都交给宿主桥，**shared 零密钥、零网络直连**。

## 2. 跨层通信：桥（BridgeModule）与 Adapter

shared 不直接碰 Android API，统一经 `base/BridgeModule`（`callNativeMethod` RPC）下发命令，宿主 `KRBridgeModule.call()` 分发执行、单次回调：

| shared 侧方法 | 宿主处理 | 用途 |
| --- | --- | --- |
| `fetchQuotes(secids)` | 腾讯行情接口 | 真实行情并入 |
| `fetchClist/fetchSectors/fetchSectorStocks` | 新浪榜单 / 板块 / 成分（板块 code 为新浪 `new_/gn_`） | 榜单、板块页 |
| `fetchKline/fetchTrends` | 腾讯 K 线 / 分时 | 图表数据 |
| `llmAnalyze(prompt,stream,sid)` | 智谱 GLM | AI 生成（可流式） |
| `showSelectableText/copyToPasteboard/toast` | 系统能力 | 复制、提示 |

其它基础能力（颜色 / 字体 / 图片 / 日志 / 线程）通过 `adapter/*`（如 `KRColorParserAdapter`）注册给渲染引擎。

## 3. 数据模型与数据源（core/）

- `Stock.kt`：`Stock` / `KLineBar` / `Sector` + 涨跌配色（`StockColor`：涨 `0xFFE54D42`、跌 `0xFF1ABE5B`）+ 指标 MA / EMA / MACD / RSI / BOLL。
- `StockData.kt`（门面）：`baseQuotes`（mock 种子兜底）+ `realPool`（经桥拉真实行情动态并入）。`loadRank/loadSectors/loadSectorStocks` 失败时保留 mock，保证演示链路不空。`DataSync.bump()` 驱动重绘。
- `UserStockStore.kt`：自选(CSV) / 隐藏；`UserSettings.kt`：主题色 / 字号 / 涨跌配色 / 迷你卡开关等偏好，持久化。
- `KRMarkdown.kt`：Markdown 解析器（块 + 行内 token）。

## 4. AI 与 LLM 子系统（core/llm/）

```
UI → LLM.client(LLM.kt)
      ├─ GLMFlashClient(真实智谱)，走 AIJobCenter(常驻根页桥)
      │     ├─ analyze(): 详情页个股 AI 分析(纯文本【】分段)
      │     └─ chat():     聊天页多轮问答(流式/非流式)
      └─ MockLLMClient(离线兜底, 零网络)
宿主 KRBridgeModule.llmAnalyze → glm-4-flash → 4.5-flash → 4.7-flash 候选链 + 整体超时兜底
```

- **LLMClient 抽象 + 双实现**：真模型 `GLMFlashClient` 与离线 `MockLLMClient` 同接口，没配 Key 或网络失败自动回退，演示不会卡死。
- **AIJobCenter 单例**：LLM 请求统一走「常驻根页面（MainTabPager）」的桥 → 子页面（聊天 / 详情 / 对比）销毁后请求照常跑，结果写单例 store 不丢。
- **真流式（SSE）**：宿主读流，把累计文本按 `sid` 写进缓存，shared 用绑前台页的定时器轮询取增量，聊天气泡逐字蹦。桥单次回调传不了多次，所以走「宿主缓存 + 轮询拉」这条通道。
- **非流式有一条整体硬超时**（`LLM_OVERALL_TIMEOUT_MS`，15s）：超时就判空回 Mock。所以要求「慢但能用」的场景（如对比页）一律走流式通道，避免被这条超时误杀。
- **持久化**：`ChatStore`（会话按股票隔离，另有独立的对比会话）、`AIAnalysisStore`（详情页 AI 结论）都落盘 SharedPreferences。
- **AI 分析结论结构化**：详情页 analyze 的提示词要求模型在开头独占一行输出 `【AI观点】风险：X｜操作建议：Y`（X ∈ 低/中/高风险，Y ∈ 买入/持有/卖出），`StockDetailPage.parseAiVerdict` 解析后在 AI 卡顶部渲染两个徽章，正文剥掉这一行。解析不到或落 Mock（Mock 也按同格式补一行）就只显示正文，不崩。
- **发散性渲染**：`StockMention.extract` 扫 AI 文本里命中行情池的股票 → 末尾渲染可点迷你走势卡，Markdown 富文本里的股票名也可点 → 跳个股详情。
- **图表选点 → AI 解读**：
  - AI 回复按 `[KCHART:代码:周期]` 指令内嵌迷你走势图（`KRChatMiniChart`，分时 / 日 / 周 / 月 / 年 K 可切）；个股详情主图 `KRKLineChart` 同样支持选点。
  - 点按吸附十字光标 → 组件把被选点（时间 / 价 / 涨跌 / 是否分时）经 `onAsk` 回调透传给外层（组件不绑股票上下文，只传点位，保持可复用）→ 外层拼成完整追问 → `RouterModule.openPage("Chat", {stockCode, prompt})` → ChatPage 收到 `prompt` 自动发出。
  - 模型偶尔不按约定写，把图写成 Markdown 图片链接（`[xx的K线图](https://…png)`），`ChatPage` 里有一层清洗把这些还原成 `[KCHART:…]`，代码必须在行情池里能解析到，免得把 CDN 数字串当股票代码。
- **AI Agent（AgentChat）**：模型决策协议，疑似操作类消息走 `AgentChat.run`，模型只输出一行 `⟦TOOL⟧{json}`，shared 解析后经 `AgentActions` 真实改 `UserSettings` / 自选 / 预警 / 对比并落盘，再问一轮拿最终答复。主聊天和对比页共用同一套工具集（9 个）。协议层有 JSON 容错、别名规范化、语义二次校准、失败短路四层兜底。

## 5. 页面与导航（app/）

- 四 Tab 主框架 `MainTabPager`（AI 聊天 / 行情 / 自选 / 我的），子 Tab 内容用 `flex + opacity` 常驻显隐。
- 业务 `@Page`：`StockDetailPage`、`SectorDetailPage`、`ChatPage`、`HeatPoolPage`、`StockComparePage`、`StockPickerPage`、`AppearancePage`、`ExpandSettingsPage`、`HiddenStocksPage`、`CreditsPage`、`FullTextPage`（新页由 KSP 自动注册；`QuotesPage` 是旧的验证页，没有入口）。
- 导航统一走 `RouterModule.openPage(name, userData)`；详情 / 聊天 / 承接都由行情或卡片点击进入，页面参数 `stockCode` / `prompt` 驱动。

## 6. 自研可复用组件（components/）

| 组件 | 职责 | 复用性 |
| --- | --- | --- |
| KRTable(KRStockList) | 行情列表 + 行内展开分页轮播（分时 / AI 分析 / 简况 / 基本面） | 脱离业务数据可复用 |
| KRKLineChart / KRMiniTimeSharing / KRTrendChart | 专业 K 线蜡烛（含指标副窗 / 缩放）/ 迷你分时 / 迷你走势 | 纯数据驱动 |
| KRChatMiniChart | AI 回复内嵌迷你走势图（分时 / 日 / 周 / 月 / 年 K 可切）+ 十字光标选点 | `onAsk` 透传点位，不绑股票上下文 |
| KRStockCard / KRStockBadge / KRRefreshButton | AI 提及股卡 / 涨跌徽标 / 下拉刷新 | 通用 |
| KRMarkdown(KRMarkdownView) | Markdown 富文本（标题 / 列表 / 引用 / 代码 / 可点股票） | 解析器（core）+ 渲染器解耦 |

## 7. 宿主 Android 侧关键约定

- `windowSoftInputMode=adjustNothing`（沉浸式 LAYOUT_FULLSCREEN，内容顶到状态栏后）。
- GLM Key 由 `local.properties` 经 `BuildConfig.GLM_API_KEY` 注入，源码零密钥。
- 真实数据源：腾讯（报价 / K 线 / 分时）+ 新浪（榜单 / 板块 / 成分）+ 东方财富（F10 基本面）；板块源已从东财迁到新浪。
- 打包时用 `com.android.tools:r8:8.5.35` 覆盖 AGP 自带的旧 R8（写在根 `build.gradle.kts` 的 buildscript 里）。AGP 7.4.2 自带的 R8 解析不了 Kotlin 2.1.21 的 class metadata，全量 dexing 会崩；平时增量编译看不出来，一打签名包就暴露。
